# Configuration email (réception — envoi de PDF par mail)
# 1. Copiez ce fichier en email.env :  cp email.env.example email.env
# 2. Remplissez les valeurs ci-dessous
# 3. Lors de l'install sur le CT, le script utilisera email.env s'il est présent
#    (placez email.env dans proxmox/docker/ avant de lancer le script, ou versionnez-le)

MAIL_FROM=noreply@votredomaine.com
SMTP_HOST=smtp.votredomaine.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre-compte@smtp.votredomaine.com
SMTP_PASS=votre_mot_de_passe

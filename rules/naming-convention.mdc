# Convention de Nommage des Classes CSS 📋

## 📌 Principes Généraux

Une hiérarchie claire et réutilisable basée sur la structure HTML :

```
section               (page complète)
  ├─ section-title    (titre de la section)
  ├─ section-contain  (conteneur principal)
  │   ├─ grid         (grille multi-colonnes)
  │   │   └─ grid-item (élément de grille)
  │   │       ├─ block         (carte/boîte autonome)
  │   │       │   ├─ block-title (titre du bloc)
  │   │       │   ├─ block-subtitle (sous-titre)
  │   │       │   └─ block-content (contenu du bloc)
  │   │       └─ action-block (bloc d'action/boutons)
  │   │           └─ block-content
  │   └─ [nom]-block  (bloc spécialisé, ex: time-block)
```

## 🎯 Classes Universelles

### Structure de Page
- `section` - Conteneur de page (article, section)
- `section-title` - Titre principal de la section
- `section-contain` - Conteneur général du contenu

### Grilles et Layouts
- `grid` - Grille CSS (flex ou grid)
- `grid-item` - Élément d'une grille (colonne)

### Blocs Réutilisables
- `block` - Carte/boîte générique (section autonome)
- `block-title` - Titre d'un bloc
- `block-subtitle` - Sous-titre dans un bloc
- `block-content` - Contenu d'un bloc
- `action-block` - Bloc contenant des actions (boutons, formulaires)

### Blocs Spécialisés
- `[name]-block` - Bloc avec un rôle spécifique
  - `time-block` - Affichage de l'heure/date
  - `card` - Alias pour `block` si plus approprié

## 📝 Exemples

### ✅ Bon Nommage

```html
<section class="home section">
    <div class="section-title">
        <h2>Accueil</h2>
    </div>
    <div class="section-contain">
        <div class="grid">
            <div class="grid-item">
                <div class="block">
                    <div class="block-title">
                        <h3>Ma Capsule</h3>
                    </div>
                    <div class="block-content">
                        <p>Contenu...</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
```

### ❌ Ancien Nommage (à éviter)

```html
<!-- NE PAS UTILISER -->
<div class="contain-1">
    <div class="capsule-info">
        <div class="capsule-title">...</div>
        <div class="capsule-content">...</div>
    </div>
</div>
```

## 🔄 Migration des Anciennes Classes

| Ancien | Nouveau | Contexte |
|--------|---------|----------|
| `grid-contain` | `grid` | Grille CSS |
| `contain-1/2/3` | `grid-item` | Élément de grille |
| `section-time` | `time-block` | Bloc d'heure/date |
| `capsule-info` | `block` | Bloc générique |
| `capsule-title` | `block-title` | Titre du bloc |
| `capsule-content` | `block-content` | Contenu du bloc |
| `capsule-subtitle` | `block-subtitle` | Sous-titre |
| `today-calendar` | `block` | Bloc générique |
| `calendar-title` | `block-title` | Titre |
| `calendar-content` | `block-content` | Contenu |
| `recent-info` | `block` | Bloc générique |
| `recent-title` | `block-title` | Titre |
| `recent-content` | `block-content` | Contenu |
| `feedback` | `action-block` | Bloc d'actions |

## 💡 Bonnes Pratiques

### ✅ À Faire
- Utiliser les classes universelles partout
- Combiner les classes pour la spécificité
- Réutiliser `block-*` pour tous les conteneurs autonomes
- Utiliser des modificateurs si nécessaire

### ❌ À Éviter
- Créer des classes spécifiques à une page (ex: `capsule-info`)
- Utiliser des noms numérotés (ex: `contain-1`)
- Mélanger les conventions
- Créer de nouvelles classes sans les justifier

## 🎨 Modificateurs (si besoin)

```html
<!-- Exemple de modificateur BEM -->
<div class="block block--highlighted">
    <div class="block-title block-title--large">...</div>
</div>
```

Mais préférer les classes universelles avant d'utiliser les modificateurs.

## 📚 Résumé Rapide

```
section
  section-title
  section-contain
    grid
      grid-item
        block (réutilisable partout)
          block-title
          block-subtitle
          block-content
        action-block
          (peut contenir block-content)
        [name]-block (spécialisé)
```

Cette convention garantit la **cohérence**, la **réutilisabilité** et la **maintenabilité** du code.

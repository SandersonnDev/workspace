# Variables d'environnement pour Workspace Client
# Copier ce fichier en .env et modifier les valeurs selon votre environnement

# Environnement (production ou development)
NODE_ENV=production

# Niveau de log (DEBUG, INFO, WARN, ERROR, NONE)
LOG_LEVEL=WARN

# Token GitHub pour publier les releases (production uniquement)
GITHUB_TOKEN=your_token_here

# Mode serveur (local, proxmox, production)
SERVER_MODE=production
